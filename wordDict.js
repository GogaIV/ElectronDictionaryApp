class Node{
  children = [];
  end = false;
}

export class WordDictionary{
  #root;

  constructor(){
    this.root = Node();
  }

  addWord(word){
    let curr = this.root;

    word.array.forEach(element => {
      idx = c - 'a';
    }); 
  }
} 